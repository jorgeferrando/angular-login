define([
    "angular"
], function (angular) {
    "use strict";
    var ctrl = ['$scope',
        function ($scope) {
            $scope.mainMsg = "Main view";
        }
    ];

    return ctrl;
});
